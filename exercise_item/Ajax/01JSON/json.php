<?php


	/**
	 * 解析JSON字符串
	 * json_encode()
	 * json_decode()
	 */
	
	$arr = array('name'=>'itcast', "age"=>10);

	$josn = '{"name": "itcast", "age": 10}';

	// echo json_encode($arr);
	// var_dump(json_decode($josn));

?>